<main id="inicio" class="w-full">
	<section class="w-full relative overflow-hidden" style="height: 100svh;">
		<!-- Slideshow Container -->
		<div class="slideshow-container w-full h-full">
			<div class="slide active">
				<img
					src="/bandido_jeans/frontis.jpg"
					alt="Bandido Jeans"
					loading="eager"
					decoding="async"
					class="w-full h-full object-cover brightness-70"
				/>
			</div>
			<div class="slide">
				<img
					src="/bandido_jeans/jeans4.avif"
					alt="Bandido Jeans - Niños"
					loading="lazy"
					decoding="async"
					class="w-full h-full object-cover brightness-70"
				/>
			</div>
			<div class="slide">
				<img
					src="/bandido_jeans/jeans2.jpg"
					alt="Bandido Jeans - Hombre"
					loading="lazy"
					decoding="async"
					class="w-full h-full object-cover brightness-70"
				/>
			</div>
			<div class="slide">
				<img
					src="/bandido_jeans/jeans.avif"
					alt="Bandido Jeans - Mujer"
					loading="lazy"
					decoding="async"
					class="w-full h-full object-cover brightness-70"
				/>
			</div>
		</div>
		
		<!-- Indicadores de slide -->
		<div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 flex gap-3 z-20">
			<button class="slide-indicator active" data-slide="0" aria-label="Slide 1"></button>
			<button class="slide-indicator" data-slide="1" aria-label="Slide 2"></button>
			<button class="slide-indicator" data-slide="2" aria-label="Slide 3"></button>
			<button class="slide-indicator" data-slide="3" aria-label="Slide 4"></button>
		</div>

		<div class="absolute inset-0 flex items-center justify-center px-4 sm:px-6 md:px-8 z-10">
			<h1 data-hero-title class="hero-title text-3xl xs:text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white text-center leading-tight drop-shadow-2xl max-w-5xl hero-letters">
				Nosotros no hacemos moda,<br class="hidden sm:block">
				hacemos jeans con identidad.
			</h1>
		</div>
	</section>

	<!-- Sección Editorial -->
	<section class="w-full bg-gray-50 py-20 sm:py-24 md:py-32 lg:py-40">
		<div class="max-w-[1600px] mx-auto px-6 sm:px-8 lg:px-12">
			<div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 xl:gap-20 items-center">
				<!-- Columna de imágenes -->
				<div class="grid grid-cols-2 gap-6">
					<div class="col-span-2 sm:col-span-1">
						<img 
							src="/bandido_jeans/jeans4.avif" 
							alt="Denim Heritage" 
							class="w-full h-[500px] sm:h-[650px] lg:h-[750px] object-cover rounded-sm shadow-2xl"
						/>
					</div>
					<div class="col-span-2 sm:col-span-1 grid grid-rows-2 gap-6">
						<img 
							src="/bandido_jeans/jeans.avif" 
							alt="Denim Detail" 
							class="w-full h-[240px] sm:h-[315px] lg:h-[365px] object-cover rounded-sm shadow-2xl"
						/>
						<img 
							src="/bandido_jeans/back2.avif" 
							alt="Craftsmanship" 
							class="w-full h-[240px] sm:h-[315px] lg:h-[365px] object-cover rounded-sm shadow-2xl"
						/>
					</div>
				</div>

				<!-- Columna de contenido -->
				<div class="space-y-12">
					<div class="border-l-4 border-red-600 pl-8">
						<blockquote class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-serif italic text-gray-900 leading-tight">
							"El denim es más que una tela: es un archivo viviente de cada movimiento, cada rebeldía."
						</blockquote>
					</div>

					<div class="grid grid-cols-1 sm:grid-cols-2 gap-8 mt-12">
						<div>
							<h3 class="text-red-600 font-bold text-sm tracking-wider mb-3">PROCESO</h3>
							<p class="text-gray-700 text-base md:text-lg leading-relaxed">
								Lavado artesanal de bajo impacto. Cada pieza recibe atención individual para lograr ese acabado auténtico.
							</p>
						</div>
						<div>
							<h3 class="text-red-600 font-bold text-sm tracking-wider mb-3">ORIGEN</h3>
							<p class="text-gray-700 text-base md:text-lg leading-relaxed">
								Diseñado y confeccionado localmente. Apoyando la industria textil con calidad y conciencia.
							</p>
						</div>
					</div>

					<div class="pt-8 border-t border-gray-300">
						<h4 class="text-2xl md:text-3xl lg:text-4xl font-bold text-gray-900 mb-3">DENIM AUTÉNTICO</h4>
						<p class="text-sm tracking-widest text-red-600 font-semibold mb-4">SELECCIÓN HERITAGE</p>
						<p class="text-gray-600 text-base md:text-lg leading-relaxed">
							Tejido en telares tradicionales. Tinte índigo puro. 
							Cada jean cuenta su propia historia a través del uso y el tiempo.
						</p>
					</div>
				</div>
			</div>
		</div>
	</section>
	
	<section class="w-full ">
		<div class="grid grid-cols-1 md:grid-cols-3 gap-0">
			<div class="relative h-[28rem] xs:h-[32rem] sm:h-[40rem] md:h-[50rem]">
				<img src="/bandido_jeans/kid.jpg" alt="Kids" class="w-full h-full object-cover" />
				<div class="absolute inset-0 flex flex-col items-center justify-center bg-black/35 text-white gap-4">
					<h3 class="text-xl sm:text-2xl font-semibold">Niños</h3>
					<a href="/bandido_jeans/galeria" class="px-5 py-2 sm:px-6 border-2 border-white text-xs sm:text-sm font-semibold hover:bg-white hover:text-black transition-colors">VER MÁS</a>
				</div>
			</div>

			<div class="relative h-[28rem] xs:h-[32rem] sm:h-[40rem] md:h-[50rem]">
				<img src="/bandido_jeans/imG/6.jpeg" alt="Kids" class="w-full h-full object-cover" />
				<div class="absolute inset-0 flex flex-col items-center justify-center bg-black/35 text-white gap-4">
					<h3 class="text-xl sm:text-2xl font-semibold">Hombre</h3>
					<a href="/bandido_jeans/galeria" class="px-5 py-2 sm:px-6 border-2 border-white text-xs sm:text-sm font-semibold hover:bg-white hover:text-black transition-colors">VER MÁS</a>
				</div>
			</div>

			<div class="relative h-[28rem] xs:h-[32rem] sm:h-[40rem] md:h-[50rem]">
				<img src="/bandido_jeans/imG/11.jpeg" alt="Mujer" class="w-full h-full object-cover" />
				<div class="absolute inset-0 flex flex-col items-center justify-center bg-black/35 text-white gap-4">
					<h3 class="text-xl sm:text-2xl font-semibold">Mujer</h3>
					<a href="/bandido_jeans/galeria" class="px-5 py-2 sm:px-6 border-2 border-white text-xs sm:text-sm font-semibold hover:bg-white hover:text-black transition-colors">VER MÁS</a>
				</div>
			</div>
		</div>
	</section>


	<!-- Banner full width con efecto parallax -->
	<div class="relative h-[60vh] sm:h-[70vh] md:h-[80vh] w-full overflow-hidden" id="parallaxBanner">
		<div class="absolute inset-0 parallax-bg">
			<img src="/bandido_jeans/jeans.avif" alt="" class="w-full h-full object-cover brightness-50 scale-110">
		</div>
		<div class="absolute inset-0 flex items-center justify-center px-4">
			<h2 class="text-white text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold drop-shadow-2xl">Prueba algo diferente.</h2>
		</div>
	</div>



</main>

<style>
	.hero-title {
		word-wrap: break-word;
		overflow-wrap: break-word;
		hyphens: none;
		word-break: keep-all;
	}

	/* Ajustes específicos para móviles pequeños */
	@media (max-width: 480px) {
		.hero-title {
			font-size: 1.5rem !important;
			line-height: 1.4;
			padding: 0 0.75rem;
			max-width: 92vw;
			letter-spacing: -0.02em;
		}
	}

	@media (min-width: 481px) and (max-width: 640px) {
		.hero-title {
			font-size: 1.85rem !important;
			line-height: 1.35;
			max-width: 95vw;
		}
	}

	:global(.hero-letters span) {
		opacity: 0;
		display: inline-block;
		transform: translateY(14px);
		animation: heroFade 0.75s ease forwards;
		animation-delay: calc(var(--i) * 0.05s);
	}

	@keyframes heroFade {
		from {
			opacity: 0;
			transform: translateY(14px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.parallax-bg {
		will-change: transform;
		transition: transform 0.1s ease-out;
	}

	/* Slideshow Styles */
	.slideshow-container {
		position: relative;
	}

	.slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		opacity: 0;
		transition: opacity 1.5s ease-in-out;
		pointer-events: none;
	}

	.slide.active {
		opacity: 1;
		pointer-events: auto;
	}

	.slide-indicator {
		width: 12px;
		height: 12px;
		border-radius: 50%;
		background-color: rgba(255, 255, 255, 0.5);
		border: 2px solid white;
		cursor: pointer;
		transition: all 0.3s ease;
	}

	.slide-indicator:hover {
		background-color: rgba(255, 255, 255, 0.8);
		transform: scale(1.2);
	}

	.slide-indicator.active {
		background-color: white;
		transform: scale(1.3);
	}
</style>

<script is:inline>
	// Animación de entrada por letra para el hero
	const heroTitle = document.querySelector('[data-hero-title]');

	if (heroTitle) {
		const nodes = Array.from(heroTitle.childNodes);
		const fragment = document.createDocumentFragment();
		let index = 0;

		nodes.forEach((node) => {
			if (node.nodeType === Node.TEXT_NODE) {
				const text = node.textContent ?? '';
				for (const char of text) {
					if (char === '\n' || char === '\r' || char === '\t') continue;
					const span = document.createElement('span');
					span.style.setProperty('--i', String(index));
					span.textContent = char === ' ' ? '\u00A0' : char;
					fragment.append(span);
					index += 1;
				}
			} else if (node.nodeType === Node.ELEMENT_NODE && node.tagName === 'BR') {
				fragment.append(node.cloneNode(true));
			}
		});

		heroTitle.textContent = '';
		heroTitle.append(fragment);
	}

	// Slideshow automático
	const slides = document.querySelectorAll('.slide');
	const indicators = document.querySelectorAll('.slide-indicator');
	let currentSlide = 0;
	let slideInterval;

	function showSlide(index) {
		slides.forEach((slide, i) => {
			slide.classList.toggle('active', i === index);
		});
		indicators.forEach((indicator, i) => {
			indicator.classList.toggle('active', i === index);
		});
		currentSlide = index;
	}

	function nextSlide() {
		const next = (currentSlide + 1) % slides.length;
		showSlide(next);
	}

	function startSlideshow() {
		slideInterval = setInterval(nextSlide, 5000);
	}

	function stopSlideshow() {
		clearInterval(slideInterval);
	}

	// Event listeners para los indicadores
	indicators.forEach((indicator, index) => {
		indicator.addEventListener('click', () => {
			stopSlideshow();
			showSlide(index);
			startSlideshow();
		});
	});

	// Iniciar slideshow
	startSlideshow();

	// Pausar en hover (opcional)
	const slideshowContainer = document.querySelector('.slideshow-container');
	if (slideshowContainer) {
		slideshowContainer.addEventListener('mouseenter', stopSlideshow);
		slideshowContainer.addEventListener('mouseleave', startSlideshow);
	}

	// Efecto parallax
	const parallaxBanner = document.getElementById('parallaxBanner');
	
	if (parallaxBanner) {
		const parallaxBg = parallaxBanner.querySelector('.parallax-bg');
		let ticking = false;
		let lastScrolled = 0;
		
		const handleScroll = () => {
			const scrolled = window.pageYOffset;
			const bannerTop = parallaxBanner.offsetTop;
			const bannerHeight = parallaxBanner.offsetHeight;
			
			// Calcula si el banner está visible en el viewport
			if (scrolled + window.innerHeight > bannerTop && scrolled < bannerTop + bannerHeight) {
				const offset = (scrolled - bannerTop) * 0.5; // Factor de velocidad del parallax
				if (parallaxBg instanceof HTMLElement) {
					parallaxBg.style.transform = `translateY(${offset}px)`;
				}
			}
		};

		const requestTick = () => {
			if (!ticking) {
				requestAnimationFrame(handleScroll);
				ticking = true;
				setTimeout(() => { ticking = false; }, 16); // ~60fps
			}
		};
		
		window.addEventListener('scroll', requestTick, { passive: true });
		handleScroll(); // Ejecutar una vez al cargar
	}
</script>